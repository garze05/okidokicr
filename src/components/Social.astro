---
import { Icon } from "astro-icon/components";

const {
  platform,
  username,
  url,
  iconName,
  icon = true,
  size = 25,
  color = "currentColor",
} = Astro.props;

const href = url || `https://www.${platform}.com/${username}`;
const ariaLabel = `Visítanos en ${platform}`;
---

<li class="drop-shadow-lg transition-transform duration-300 hover:scale-120">
  <a
    href={href}
    target="_blank"
    rel="noopener noreferrer"
    aria-label={ariaLabel}
    class="inline-block"
  >
    {
      icon && (
        <Icon
          name={iconName}
          width={size}
          height={size}
          class={`text-${color} transition-color hover:text-primary-500 duration-500`}
          aria-hidden="true"
        />
      )
    }
    {!icon && <span class="sr-only">{platform}</span>}
  </a>
</li>
